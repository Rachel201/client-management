import { createTheme } from 'office-ui-fabric-react';
import { addVariants } from '@uifabric/variants';
export var DefaultTheme = createTheme();
addVariants(DefaultTheme);
export var DefaultCustomizations = {
    settings: {
        theme: DefaultTheme,
    },
    scopedSettings: {},
};
//# sourceMappingURL=DefaultCustomizations.js.map