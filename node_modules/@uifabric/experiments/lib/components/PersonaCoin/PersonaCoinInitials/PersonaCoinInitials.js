import * as React from 'react';
import { Icon, Text } from 'office-ui-fabric-react';
import { getInitials, getRTL } from '../../../Utilities';
export var PersonaCoinInitials = function (props) {
    var initials = (typeof props.initials === 'string' && props.initials) ||
        getInitials(props.text, getRTL(), props.allowPhoneInitials);
    if (initials) {
        return React.createElement(Text, { className: props.className }, initials);
    }
    return React.createElement(Icon, { iconName: "Contact", className: props.className });
};
//# sourceMappingURL=PersonaCoinInitials.js.map