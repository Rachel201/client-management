/** @jsx withSlots */
import { PersonaCoin } from '../../PersonaCoin/PersonaCoin';
import { getSlots, withSlots } from '../../../Foundation';
import { PersonaText } from '../PersonaText/PersonaText';
export var VerticalPersonaView = function (props) {
    var Slots = getSlots(props, {
        root: 'div',
        primaryText: PersonaText,
        secondaryText: PersonaText,
        coin: PersonaCoin,
    });
    return (withSlots(Slots.root, null,
        withSlots(Slots.coin, { text: props.text }),
        withSlots(Slots.primaryText, null, props.text),
        withSlots(Slots.secondaryText, null)));
};
//# sourceMappingURL=VerticalPersona.view.js.map